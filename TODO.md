# 待辦與工單清單

本清單源自 2026-08-31 對 `ShinyColors-enza-userscript-Traditional-Mandarin.user.js` 的架構審查。

**2026-08-31 更新**：本機已安裝 Node.js v24.19.0，先前所有「無法實際驗證」的缺口皆已補齊，各項的驗證結果記於下方。

---

## 已完成

### [x] TODO-01：字型注入在 `document-start` 時序下可能整段失效

- **位置**：`builder.js` 的 `injectOnlineHuninn()` 區塊
- **問題**：`@run-at document-start` 執行時 `document.head` 很可能尚未存在。原本寫 `document.head.appendChild(gFont);` 沒有 null 檢查，一旦拋出例外會被外層空的 `catch(e){}` 吞掉，導致整個字型注入（含 `@font-face` 與 `FontFace()` 載入）全部失敗且無任何錯誤訊息。
- **修法**：改成 `(document.head || document.documentElement).appendChild(gFont);`，與同一函式內 `styleEl` 原本就有的降級寫法一致。
- **驗證**：`node builder.js` 編譯成功、`node --check` 語法通過，並確認降級寫法確實出現在編譯產物中。
- **仍待確認**：瀏覽器實機行為（字型有沒有真的套上）需人工開遊戲確認，靜態檢查無法涵蓋。

### [x] TODO-02：`builder.js` 對正則替換失敗沒有防呆

- **問題**：`oldJiPattern`/`oldWiPattern` 抓不到目標時，`.replace()` 會靜默不做事，輸出一份「看似成功但 hook 沒生效」的檔案。
- **🔴 第一版修法是錯的（記錄下來避免重蹈）**：最初寫成「比對不到舊樣式就中止」，實際跑起來直接把正常編譯擋掉。
  **根因**：`oldWiPattern` 找的 `document.body.appendChild(e);` 只存在於**未注入過的上游原始碼**。builder.js 跑過一次後該段文字就消失了，比對不到是**正常的冪等 no-op，不是錯誤**。
  **教訓**：檢查該驗「輸出有沒有該有的特徵」（結果），不是「有沒有比對到輸入樣式」（機制）。對冪等的建置流程，機制檢查必然誤報。
- **正式修法**：改為完成度檢查，替換完之後斷言輸出具備 `TW_FONT_FAMILY`（ji 的字型注入）與 `Wi=async()=>`（Wi 的字型載入攔截），缺任一項才中止。
- **驗證**：連續執行 `node builder.js` 兩次皆 exit=0（冪等性成立），且下方負向測試證明它擋得住真正的失效。

### [x] TODO-03：`builder.js` 沒有「乾淨上游原始碼」可回溯

- **問題**：`builder.js` 讀的是即將被覆寫的輸出檔本身，repo 內沒有保存任何一份未注入過的原廠遊戲程式碼。遊戲官方若更新 `ji`/`Wi` 邏輯，現有正則會抓不到，且沒有基準版本可 diff。
- **處理**：這本質是流程問題而非可一次改完的程式碼問題。已在讀檔那行前加上警語註解，把「遇到官方更新導致比對失敗時，先手動存一份原始快照再動手」寫進程式碼本身。

### [x] TODO-04：`s2t()` 呼叫點是手動逐一包出來的，沒有集中稽核機制

- **風險**：新增功能或上游更新帶進新的中文字串時，若忘記包 `s2t()`，畫面會出現「這裡怎麼還是簡體」且難以定位。
- **處理**：新增 `audit-s2t-coverage.js`，掃描 `.text=`/`.title=`/`.name=`/`.select=`/`.trans=` 五種賦值點，往右看 20 字元內有無 `s2t(`，列出「已包／疑似未包」供人工複核。
- **執行結果**：11 個賦值點，5 個已包 `s2t()`，6 個列為待確認。
- **6 個待確認項的人工判定結果**：全部是角色 ID 對應、CSV 欄位鍵、`Error` 子類別名稱（`"InvalidCharacterError"`、`"InvalidTokenError"` 這類英文常數），**不是需要繁簡轉換的中文欄位，維持原樣是對的**。
- **交叉驗證**：腳本統計出的數字（`.text=` 2/2、`.name`+`.select`+`.title` 3/9）與另外用 Grep 獨立人工核對的結果完全一致，兩種方法互相印證。
- **結論**：目前版本沒有真正的漏轉。本腳本的價值在未來新增賦值點時的迴歸檢查。
- **已知限制**：這是啟發式掃描不是語法解析，只認上述五種欄位名，換成別的欄位名就掃不到。

### [x] TODO-05：`s2t()` 沒有結果快取

- **處理**：加入模組層級的 `s2tCache = new Map()`，命中直接回傳，未命中才跑轉換再存入。未做 LRU 或容量上限，因為遊戲文字是有限詞彙集合不會無限增長（YAGNI）。
- **驗證**：以同一份字典各建「有快取版」與「無快取版」，餵 11,276 筆輸入（含所有詞條、所有單字、詞條相接、空字串／null／數字／物件等邊界值），且每筆都跑兩輪以確保涵蓋快取命中路徑。**輸出不一致筆數：0**。

### [x] TODO-06：詞組正則的交替順序造成死規則

- **問題**：`regexPhrases` 是 `PHRASES.map(...).join('|')`，而 JS 正則交替取「**最先列出的**」而非「最長的」。只要某個短詞排在以它為前綴的長詞前面，長詞就永遠比對不到。實測有 **5 條**「关系」開頭的詞被這樣遮蔽。
- **影響**：對現況是無害的（那 5 條的詞組級結果剛好與逐字轉換相同），但這是**潛在風險**：往後新增詞條時，只要順序不對就會靜默失效。
- **修法**：在 `builder.js` 組正則之前，將 `dict.phrases` 依 key 長度**降冪排序**。
- **驗證**：被遮蔽數 **5 → 0**；差異測試顯示**受影響而改變輸出的輸入數為 0**，確認此改動純屬預防、未動到任何現有行為。

### [x] TODO-07：字典平行陣列格式沒有防呆

- **問題**：`sChars`/`tChars` 是兩條必須等長、同索引對應的平行陣列，這個格式**抓不出自己的錯位**。commit `b4d5be1` 的「艺 → 豔」就是手動編輯時兩邊長度沒對齊造成整批錯位。
- **修法**：`builder.js` 編譯前檢查三項並在不通過時中止：兩表等長、`sChars` 無重複字元、`phrases` 無重複詞條。
- **驗證**：見下方負向測試。

---

## 守門的負向測試（`guard-negative-test.js`）

全綠分不出「沒問題」與「根本抓不到」，所以另外在暫存沙盒裡注入真實失誤形態驗證守門確實會擋。**5 個案例全數通過**：

| 案例 | 注入的錯誤 | 期望 | 結果 |
|---|---|---|---|
| A | 不注入錯誤（基準組） | 編譯成功 | 通過 |
| B | `sChars`/`tChars` 長度不一致 | 中止 | 通過 |
| C | `sChars` 出現重複字元 | 中止 | 通過 |
| D | `phrases` 出現重複詞條 | 中止 | 通過 |
| E | Wi 字型攔截消失 | 中止 | 通過 |

B 案即 `b4d5be1` 真實發生過的事故形態，現在會被擋下。測試腳本在暫存區複製沙盒執行，不觸碰 repo。

---

## 已確認無需處理

- `escapeRegExp()` 沒轉義 `-`：確認過 `-` 在字元類別外不是特殊字元，不影響正確性。

---

## 待辦

### [ ] TODO-08：簡繁字典體檢與台灣正體化

已完成規劃，尚未動工。重點結論：

- **字表缺口 1,309 字**：OpenCC `STCharacters.txt` 有 4,012 個簡體字，本專案 `sChars` 只有 2,709。缺的是長尾罕用字（常用字皆已收錄），但長尾字會出現在人名、成語、招式名裡。
- **`抬 → 擡` 是錯誤規則**：`抬` 本身即台灣標準字。與先前已手動移除的 `吃 → 喫`、`占 → 佔` 同類。
- **`撻 → 塔` 高度可疑**：`撻` 本身已是繁體字，映射成語意無關的 `塔` 會毀掉文字。此規則在 `b4d5be1` 新增，疑似錯位殘渣。
- **一簡多繁的預設值**：OpenCC 標出 277 個一簡多繁字。其中 `干` 目前固定映射成「乾」，但權威默認是「幹」，導致 `干什么`／`干活`／`干得好` 會被誤轉；`后` 方向正確但缺 `皇后`／`太后`／`后宮` 等例外詞。
- **方向已裁示**：字形對齊台灣正體、一併做大陸用語本土化。

### [ ] TODO-09：實機驗證

`node --check` 只能證明語法正確，證明不了畫面正常。需人工開遊戲確認文字轉繁體與字型套用皆正常。

---

## 本次一併處理的重大事故（已修復並上線）

`.user.js` 的區塊註解未正確閉合，導致第 25 行至第 129 行整段（含整個繁簡轉換引擎與字型注入）被吃進註解，腳本完全無法執行。損毀起點為 `b4d5be1`，與上述 `艺 → 豔` 錯位屬同一次編輯事故。已於 commit `5a75ae3` 單獨修復並推上 `main`，驗證方式為 `node --check` 前後對照（修復前 exit=1 `SyntaxError`，修復後 exit=0）。
