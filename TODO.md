# 待辦與工單清單

本清單源自 2026-08-31 對 `ShinyColors-enza-userscript-Traditional-Mandarin.user.js` 的架構審查。

**2026-08-31 更新**：本機已安裝 Node.js v24.19.0，先前所有「無法實際驗證」的缺口皆已補齊，各項的驗證結果記於下方。

---

## 已完成

### [x] TODO-01：字型注入在 `document-start` 時序下可能整段失效

- **位置**：`builder.js` 的 `injectOnlineHuninn()` 區塊
- **問題**：`@run-at document-start` 執行時 `document.head` 很可能尚未存在。原本寫 `document.head.appendChild(gFont);` 沒有 null 檢查，一旦拋出例外會被外層空的 `catch(e){}` 吞掉，導致整個字型注入（含 `@font-face` 與 `FontFace()` 載入）全部失敗且無任何錯誤訊息。
- **修法**：改成 `(document.head || document.documentElement).appendChild(gFont);`，與同一函式內 `styleEl` 原本就有的降級寫法一致。
- **驗證**：`node builder.js` 編譯成功、`node --check` 語法通過，並確認降級寫法確實出現在編譯產物中。
- **仍待確認**：瀏覽器實機行為（字型有沒有真的套上）需人工開遊戲確認，靜態檢查無法涵蓋。

### [x] TODO-02：`builder.js` 對正則替換失敗沒有防呆

- **問題**：`oldJiPattern`/`oldWiPattern` 抓不到目標時，`.replace()` 會靜默不做事，輸出一份「看似成功但 hook 沒生效」的檔案。
- **🔴 第一版修法是錯的（記錄下來避免重蹈）**：最初寫成「比對不到舊樣式就中止」，實際跑起來直接把正常編譯擋掉。
  **根因**：`oldWiPattern` 找的 `document.body.appendChild(e);` 只存在於**未注入過的上游原始碼**。builder.js 跑過一次後該段文字就消失了，比對不到是**正常的冪等 no-op，不是錯誤**。
  **教訓**：檢查該驗「輸出有沒有該有的特徵」（結果），不是「有沒有比對到輸入樣式」（機制）。對冪等的建置流程，機制檢查必然誤報。
- **正式修法**：改為完成度檢查，替換完之後斷言輸出具備 `TW_FONT_FAMILY`（ji 的字型注入）與 `Wi=async()=>`（Wi 的字型載入攔截），缺任一項才中止。
- **驗證**：連續執行 `node builder.js` 兩次皆 exit=0（冪等性成立），且下方負向測試證明它擋得住真正的失效。

### [x] TODO-03：`builder.js` 沒有「乾淨上游原始碼」可回溯

- **問題**：`builder.js` 讀的是即將被覆寫的輸出檔本身，repo 內沒有保存任何一份未注入過的原廠遊戲程式碼。遊戲官方若更新 `ji`/`Wi` 邏輯，現有正則會抓不到，且沒有基準版本可 diff。
- **處理**：這本質是流程問題而非可一次改完的程式碼問題。已在讀檔那行前加上警語註解，把「遇到官方更新導致比對失敗時，先手動存一份原始快照再動手」寫進程式碼本身。

### [x] TODO-04：`s2t()` 呼叫點是手動逐一包出來的，沒有集中稽核機制

- **風險**：新增功能或上游更新帶進新的中文字串時，若忘記包 `s2t()`，畫面會出現「這裡怎麼還是簡體」且難以定位。
- **處理**：新增 `audit-s2t-coverage.js`，掃描 `.text=`/`.title=`/`.name=`/`.select=`/`.trans=` 五種賦值點，往右看 20 字元內有無 `s2t(`，列出「已包／疑似未包」供人工複核。
- **執行結果**：11 個賦值點，5 個已包 `s2t()`，6 個列為待確認。
- **6 個待確認項的人工判定結果**：全部是角色 ID 對應、CSV 欄位鍵、`Error` 子類別名稱（`"InvalidCharacterError"`、`"InvalidTokenError"` 這類英文常數），**不是需要繁簡轉換的中文欄位，維持原樣是對的**。
- **交叉驗證**：腳本統計出的數字（`.text=` 2/2、`.name`+`.select`+`.title` 3/9）與另外用 Grep 獨立人工核對的結果完全一致，兩種方法互相印證。
- **結論**：目前版本沒有真正的漏轉。本腳本的價值在未來新增賦值點時的迴歸檢查。
- **已知限制**：這是啟發式掃描不是語法解析，只認上述五種欄位名，換成別的欄位名就掃不到。

### [x] TODO-05：`s2t()` 沒有結果快取

- **處理**：加入模組層級的 `s2tCache = new Map()`，命中直接回傳，未命中才跑轉換再存入。未做 LRU 或容量上限，因為遊戲文字是有限詞彙集合不會無限增長（YAGNI）。
- **驗證**：以同一份字典各建「有快取版」與「無快取版」，餵 11,276 筆輸入（含所有詞條、所有單字、詞條相接、空字串／null／數字／物件等邊界值），且每筆都跑兩輪以確保涵蓋快取命中路徑。**輸出不一致筆數：0**。

### [x] TODO-06：詞組正則的交替順序造成死規則

- **問題**：`regexPhrases` 是 `PHRASES.map(...).join('|')`，而 JS 正則交替取「**最先列出的**」而非「最長的」。只要某個短詞排在以它為前綴的長詞前面，長詞就永遠比對不到。實測有 **5 條**「关系」開頭的詞被這樣遮蔽。
- **影響**：對現況是無害的（那 5 條的詞組級結果剛好與逐字轉換相同），但這是**潛在風險**：往後新增詞條時，只要順序不對就會靜默失效。
- **修法**：在 `builder.js` 組正則之前，將 `dict.phrases` 依 key 長度**降冪排序**。
- **驗證**：被遮蔽數 **5 → 0**；差異測試顯示**受影響而改變輸出的輸入數為 0**，確認此改動純屬預防、未動到任何現有行為。

### [x] TODO-07：字典平行陣列格式沒有防呆

- **問題**：`sChars`/`tChars` 是兩條必須等長、同索引對應的平行陣列，這個格式**抓不出自己的錯位**。commit `b4d5be1` 的「艺 → 豔」就是手動編輯時兩邊長度沒對齊造成整批錯位。
- **修法**：`builder.js` 編譯前檢查三項並在不通過時中止：兩表等長、`sChars` 無重複字元、`phrases` 無重複詞條。
- **驗證**：見下方負向測試。

---

## 守門的負向測試（`guard-negative-test.js`）

全綠分不出「沒問題」與「根本抓不到」，所以另外在暫存沙盒裡注入真實失誤形態驗證守門確實會擋。**5 個案例全數通過**：

| 案例 | 注入的錯誤 | 期望 | 結果 |
|---|---|---|---|
| A | 不注入錯誤（基準組） | 編譯成功 | 通過 |
| B | `sChars`/`tChars` 長度不一致 | 中止 | 通過 |
| C | `sChars` 出現重複字元 | 中止 | 通過 |
| D | `phrases` 出現重複詞條 | 中止 | 通過 |
| E | Wi 字型攔截消失 | 中止 | 通過 |

B 案即 `b4d5be1` 真實發生過的事故形態，現在會被擋下。測試腳本在暫存區複製沙盒執行，不觸碰 repo。

---

## 已確認無需處理

- `escapeRegExp()` 沒轉義 `-`：確認過 `-` 在字元類別外不是特殊字元，不影響正確性。

---

## 待辦

### [x] TODO-08 第一階段：單字表體檢與台灣正體化（不需語料的部分）

基準：OpenCC `STCharacters.txt`（決定哪些是簡體字與預設繁體字）＋ `TWVariants.txt`（繁體轉台灣字形，只能當第二道工序）。

**改動內容（`sChars`/`tChars` 2,709 → 2,715 字）**

| 類別 | 數量 | 內容 |
|---|---|---|
| 舊字形改台灣字形 | 20 | `爲→為`、`衆→眾`、`啓→啟`、`牀→床`、`竈→灶`、`癡→痴`、`羣→群`、`麪→麵`、`脣→唇` 等 |
| 移除錯誤規則 | 2 | `抬→擡`、`撻→塔` |
| 修正明確錯字 | 2 | `忾`：`儾→愾`、`咝`：`嘶→噝` |
| 新增缺口字 | 8 | 含 `黄→黃`、`羁→羈` |

**最有價值的是那 20 個舊字形。** 根因是現行字典建自 OpenCC 的「通用繁體」而非「台灣正體」，一個原因解釋全部 20 個。`为→爲` 影響尤其大，等於每個「因為」都顯示成舊字形。

**🔴 兩個踩到的陷阱（記錄下來避免重蹈）**

1. **「1,309 字缺口」名不副實。** 拆解後：1,141 個的來源字本身就是超罕見的擴充區字、27 個目標字超出基本平面、132 個是一簡多繁（多數映射到自己）、1 個映射到自己，**真正可用的只有 8 個**。教訓是報數字前要先看內容。
2. **差點重演 `b4d5be1` 的平行陣列錯位。** OpenCC 資料含基本多文種平面以外的字，那類字在 JS 佔兩個碼元，而 `sChars`/`tChars` 是**逐碼元對應**的，混進一個就會讓該位置之後整批錯開。第一次試跑產生了 `sChars 5024 / tChars 4236` 的長度不一致，**是 TODO-07 加的完整性檢查擋下來的**。此格式先天無法表示這類字，已在補字時排除。

**刻意不做的部分（每個都有理由）**

- 132 個一簡多繁缺口全部跳過。其中會實際改變文字的 5 個都是地雷：`占→佔` 正是先前手動移除過的規則、`里→裡` 會讓「公里」變「公裡」、`郁→鬱` 會改掉人名用字。
- `秘→祕` 改成 `秘` 是依 OpenCC 台灣表，但教育部標準用 `祕`，此條可討論。
- `䴙`、`棂`、`毶`、`诶` 四字的選字分歧屬變體偏好、無明確依據，維持原樣。

**驗證**：18 項行為測試全通過（含「里」「占」應維持不轉的兩個反向案例）、`node builder.js` 通過完整性檢查、`node --check` 通過、守門負向測試 5/5 回歸通過。

### [x] TODO-08 第二階段：一簡多繁的預設值取捨（語料佐證）

語料來源：上游 `biuuu/ShinyColors` 的 `gh-pages/data/story/`（5,619 CSV、30.45 MB）。
只統計中文譯文欄 `trans`，**不碰日文原文欄 `text`**（日文同樣使用 干／栗／托／熏 等漢字，混進來會讓統計失真）。
語料規模：243,513 筆有譯文的資料列、3,918,457 字元。

**`干`：預設值方向錯誤，已修正（`乾` → `幹`）**

語料中 `干` 出現 1,474 次，真實分布為 **幹 1,077 ／ 乾 374 ／ 干 23**。原本的預設值押在只佔 25% 的那一邊。

| | 正確 | 錯誤 | 正確率 |
|---|---|---|---|
| 改動前（預設 `乾`） | 551 | 923 | 37.4% |
| 改動後（預設 `幹`） | 1,449 | 25 | **98.3%** |

同時新增 40 條詞組處理「乾」類例外（`干脆`／`干净`／`饼干`／`干杯`／`擦干` 等，皆附語料實際出現次數）。

**`栗`：預設值方向錯誤，已修正**

語料中 `栗` 出現 22 次，`栗子`(16)／`板栗`／`醋栗` 等佔絕大多數，僅 `战栗`／`而栗` 共 3 次屬「慄」義。原本固定轉成 `慄`，會讓「栗子」變成「慄子」。
修法：`栗` 整條移出逐字表（本身即繁體，比照 `里`／`面`／`只` 的既有處理），另補 4 條「慄」義詞組。

**`托`：語料推翻了權威建議，維持現狀**

OpenCC 建議預設 `托`，但語料中 `拜托`(880)＋`委托`(91) 遠多於 `摩托`(15)＋`衬托`(6)。**現行的 `託` 才是對的**，不改。這是「不能盲從權威表、要看實際語料」的具體案例。

**`熏`／`吣`／`硷`：證據不足，維持現狀**
`熏` 僅 5 次（`熏香` 4 次），`吣`／`硷` 在語料中 0 次。

**同時修掉 8 條因執行順序而永遠不生效的死詞組**

`s2t()` 是「先跑詞組、再逐字轉換」，但這 8 條的 key 是**已轉換後的繁體形態**，面對簡體輸入永遠比對不到。原作者的意圖是「事後補救」，但引擎沒有那個階段。
修法：移除死條目，改補以簡體為 key 的可用版本。其中 `联系 → 聯繫`（語料 291 次）與 `轻松 → 輕鬆`（199 次）影響最大。
`乾勁`／`乾嘛`／`乾什` 三條在 `干 → 幹` 之後自然就正確，不需替代條目。

### 🔴 已知架構限制：詞組的輸出不能包含逐字表的 key

**已實測確認**（非推論）：

```
干扰 → 幹擾   （期望 干擾）✗
皇后 → 皇後   （期望 皇后）✗
干脆 → 乾脆   ✓
饼干 → 餅乾   ✓
```

原因：詞組替換完成後，逐字轉換會再掃一次輸出。`乾` 不在逐字表中所以留得住，`干`／`后` 在表中所以會被再轉一次。

**受影響的情況**：
- `干擾`／`干涉`／`干預`／`若干` 這類本身就該維持 `干` 的詞（語料 23 次，佔 `干` 總數 1.6%）
- `皇后`／`太后`／`后宮` 這類帝后詞（語料中 `皇后` 僅 2 次，`后` 總計 7,221 次幾乎全是「後」義）

**處置：接受，不修。** 要修得替 `s2t()` 加一個「逐字轉換之後再跑一次的詞組階段」，為了 1.6% 的案例動引擎架構不划算。若未來這類詞變多，再評估。

### [x] TODO-10：大陸用語本土化

**先更正一個事實**：舊版 OpenCC 分成 `TWPhrasesIT/Name/Other` 三檔，現行 master **已合併為單一 `TWPhrases.txt`**（實查目錄確認）。

**盤點結果**：`TWPhrases.txt` 共 818 條，語料命中 139 條。但這份詞庫**本質上是資訊工程術語庫**，直接套用在偶像養成遊戲的對話上會出大事。逐條檢視語境後只採用 32 條。

**🔴 若無腦全收會發生什麼（語料實證）**

| 詞條 | 語料次數 | 套用後 | 問題 |
|---|---|---|---|
| `支持 → 支援` | 430 | 「全力支援你」 | 語料全是「全力支持你」「一直支持著我」的**應援義** |
| `类型 → 型別` | 109 | 「喜歡的型別」 | 語料是「那種類型的人」「服裝類型」 |
| `对象 → 物件` | 91 | 「結婚物件」 | 語料是「結婚對象」「練習對象」「說話對象」，**全部指人** |
| `菜单 → 選單` | 56 | 餐廳的「選單」 | 語料是「情人節菜單」「看菜單上的」 |
| `高级 → 高階` | 41 | 「高階和牛」 | 語料是「高級烤肉」「高級和牛」「高級懷石」 |
| `查看 → 檢視` | 32 | 「查檢視吧」 | 語料有「查查看吧」，**替換後直接壞掉** |
| `脚本 → 指令碼` | 7 | 「分鏡指令碼」 | 語料是「按照腳本」「分鏡腳本」「腳本家」，指**劇本** |

**✅ 2026-08-31 裁示：這 27 條全部不套用。** 完整清單與理由列在文末「裁示紀錄」一節，不要再逐條重新爭論。

**已採用的 32 條**（物品名／專有名詞／台灣固定譯法，任何語境都不會出錯），涵蓋語料 1,100 處：
`视频→影片`(331)、`咖喱→咖哩`(82)、`录像→錄影`(77)、`屏幕→螢幕`(51)、`出租车→計程車`(50)、`网络→網路`(48)、`塑料→塑膠`(43)、`信号→訊號`(41)、`短信→簡訊`(41)、`自行车→腳踏車`(40)、`相册→相簿`(31)、`搜索→搜尋`(30)、`软件→軟體`(26)、`土豆→馬鈴薯`(12) 等。

### [x] TODO-11：麵義詞與裡義詞的缺口

`面` 與 `里` 都刻意不在逐字表中（避免「面對」變「麵對」、「公里」變「公裡」），但這也讓麵義／裡義的詞跟著沒被處理。

- **麵義**：新增 20 條，涵蓋 337 處。最大宗是 `面包 → 麵包`(151)、`荞麦面 → 蕎麥麵`(77)、`拉面 → 拉麵`(25)
- **裡義**：新增 29 條，涵蓋 3,164 處。最大宗是 `哪里 → 哪裡`(938)、`那里 → 那裡`(706)、`里面 → 裡面`(480)、`家里 → 家裡`(285)、`心里 → 心裡`(224)

**設計上刻意只收「具體名詞＋里」的形式，不用「里＋助詞」的泛用形式**，避免與人名相撞（詳見下一項）。

### [x] TODO-12：詞組表輸出值的舊字形

先前只修了逐字表，漏了詞組表。掃描後發現 **134 條詞組的輸出值含舊字形**，已修正 128 條：
`裏→裡`(71 條)、`麪→麵`(28 條)、`爲→為`(15 條)、`衆→眾`(6 條)、`着→著`(3 條)、`泄→洩`(2 條)、`啓→啟`(2 條) 等。

6 條因安全檢查跳過（輸出會含逐字表 key）。**其中 5 條其實是誤判**：`唇`／`群` 在逐字表中現為恆等映射，轉了等於沒轉，不會出問題。但那 5 條是語料 0 次的冷僻成語與地名，不值得為此再跑一輪。檢查本身偏保守是可接受的。

---

### [x] TODO-13：`里`／`裡` 的完整解決（原列為待裁示，實測後發現不必取捨）

原本把這題寫成「角色名 vs 長尾詞」的兩難，需要人工裁示。**那個兩難是我推論錯誤造成的，實測後不存在。**

錯在一個沒驗證的前提：以為要靠逐字表把 `里` 轉成 `裡`，而逐字表一動就會毀掉「西城樹里」。實際跑過之後：

1. **`里` 根本不在逐字表裡**（`sChars.includes('里') === false`），所以詞組輸出的「樹里」不會被逐字階段再吃一次。先前「無法用詞組保護這個名字」的說法是錯的，那個限制只對 `干` 這種本身是逐字表 key 的字成立。
2. **JS 交替是位置優先**：長度相同時，先命中的是位置較前者。所以只要有 `树里` 這條規則，`树里的` 就輪不到 `里的` 命中。已實測。

於是架構變成**保護詞優先 ＋ 泛用規則**，不動逐字表：

| 類別 | 條數 | 涵蓋語料 |
|---|---|---|
| 保護詞（`树里`／`樹里`／`公里`／`千里`／`里長`⋯） | 11 | 1,016 處 |
| 泛用規則 `里`＋後字（`里的`／`里有`／`里是`⋯） | 82 | 4,293 處 |
| 泛用規則 前字＋`里`（`子里`／`间里`／`海里`⋯） | 33 | 961 處 |

**全語料驗證**：8,576 處 `里` 中，7,147 處（**83.3%**）正確轉成「裡」；角色名「樹里」979 處，錯字「樹裡」**0 處**。

兩個查證過程中修掉的坑：

- **說話者欄位是日文原文，本來就寫作漢字「樹里」**。只保護簡體的 `树里` 會漏掉它，實測殘留一處錯字。兩種形態都要保護。
- **`里加`（拉脫維亞）不可保護**：語料 14 處全是「這裡加上」「往牛奶裡加」，保護反而製造錯字。

人名清單來源：掃過全部 5,619 個 CSV 的說話者欄位，含「里」的名字**只有「樹里」一個**（其餘皆為 `樹里＆凛世` 這類組合）。`海里` 語境全數為裡義（海裡／腦海裡），故不列入保護。

**尚未解決的仍是 `干擾`／`皇后` 那組**（`干`／`后` 本身是逐字表 key，詞組保護不了），要根治得替 `s2t()` 加一個逐字轉換之後的詞組階段。語料共 23 處，佔比 1.6%。

### [x] TODO-14：對照表回頭抓到的三批錯字

為了讓人工判斷「幹」的語感，把語料裡每一處 `干` 用完整 `s2t()` 邏輯逐句對位跑一遍、依實際輸出分組列表。**這張表本來是給人看的，結果順手抓到三批自己的錯**：

1. **漏收的乾義詞 9 種**：`梅干→梅乾`(10)、`晾干→晾乾`(10)、`干花→乾花`(9)、`鱼干→魚乾`(8)、`干烧→乾燒`(5)、`喉咙干`／`嘴巴干`／`嗓子干`／`吃干抹净`。另補 5 條語料 0 次的同類詞備用。合計 14 條。
2. **`在线 → 線上` 的切分單位錯了**：這條規則把「在」吃掉，「在**线上**会议」會變成「線上上會議」。正確的原子是「线上」不是「在线」。已改成 `线上→線上`、`线下→線下`。
3. **一條誤判**：`干花壁纸` 的測試我期望「乾花壁紙」，實際輸出「乾花桌布」。查語料後確認 `壁纸` 全部是手機／活動桌布的語境，規則本身正確，**錯的是我造的測試句**。

方法上值得記一筆：**第 1、2 批都不是靠審規則發現的，是靠把輸出攤開來看發現的。** 規則清單看起來都很合理，錯誤只在實際輸出裡現形。

### [x] TODO-15：一簡多繁的選字缺口（2026-09-01）

#### 先更正一個被誤傳的數字

先前講的 **98.3% 不是字典的正確率，那只是「干」這一個字的**。之所以會被記成整體正確率，是因為報告時沒有講清楚範圍。真正的品質要分兩個指標看，而且兩者差很多。

#### 指標一：簡體殘留（使用者訂的第一優先）— 實質已達成

判準用 OpenCC 的 `STCharacters.txt` 當**外部基準**，不用本專案的字典當判準（那是循環論證）。

🔴 **量測時踩到的坑**：第一版判準把「凡是 STCharacters 的 key 都算簡體字」，得出 83.7% 這個嚇人的數字。**那是錯的** —— OpenCC 把「了 → 了/瞭」也收成一條，但「了」本身就是合法繁體，58,094 次全是假警報。

**正確判準是「候選清單不含它自己」**（真簡體字 3,809 個，其餘 203 條是一簡多繁、預設值就是自己）：

| | |
|---|---|
| 轉換前 | 682,897 處 |
| 轉換後 | 1,337 處 |
| **消除率** | **99.80%** |

而剩下的 1,337 處**全部不是錯字**，只有 6 種字，全是台灣正體與通用繁體的字形差異：`峰`（OpenCC 要 `峯`）832、`秘`（要 `祕`）233、`床`（要 `牀`）137、`群`（要 `羣`）125、`痴`（要 `癡`）6、`灶`（要 `竈`）4。前者皆為台灣標準字形，維持不動。

#### 指標二：選字（該轉成另一個繁體字卻沒轉）— 本次處理的部分

用台灣慣用詞試打 68 條，50 條沒接住；在轉換後的輸出中實際重數為 **1,641 處、41 種詞**。

**已補 57 條詞組 ＋ 2 條保護詞**，剩餘 84 處。最大宗的修正：

`制作→製作`、`杂志→雜誌`、`想象→想像`、`放松→放鬆`、`下周→下週`、`仿佛→彷彿`、`录制→錄製`、`困了→睏了`、`老板→老闆`、`呆在→待在`、`借口→藉口`、`舍弃→捨棄`、`向往→嚮往`、`手表→手錶`、`喂食→餵食`、`注册→註冊`

#### 🔴 動筆前逐條看語境，攔下四個反例

**這四條若照詞頻直接加，會製造比原本更多的錯字**：

| 提案 | 語料實證 | 處置 |
|---|---|---|
| `象是 → 像是` | 26 處**全部**是「對象是／印象是／想象是」，**無一是「好象是」** | 排除。改收 `想象 → 想像` |
| `自制 → 自製` | 兩義並存：「自製的點心」「自製麵包店」是製造義，「自然會自制」「該自制比較好」是克制義 | 排除。與 `干擾` 同屬無法一律替換的類型 |
| `一周 → 一週` | 有一處是「旋轉了一周」＝圈數 | 加保護詞 `转了一周` |
| `呆在 → 待在` | 有一處是「站著發呆在想什麼」 | 加保護詞 `发呆在` |

另查證三個「短詞被包在長詞裡」的陷阱，語料實測 `选手表`／`控制作`／`限制作`／`抑制作`／`录制作` **皆為 0 次**，故 `手表` 與 `制作` 可安全加入。

#### 裁示（2026-09-01）：以下維持不改

`布置`（教育部標準即為「布置」）、`分布`、`念书`／`念出来`（兩者台灣皆通行）、`背包`（台灣更常見）、`克制`（台灣同樣使用）、`出戏`（此語境是跳脫角色，不是量詞）。

#### 剩餘 84 處的組成（**全部不是錯字**）

`布置` 23、`背包` 16、`念出來` 3、`念書` 1 為裁示保留；`象是` 20 與 `自制` 19 是量測腳本的標籤過時（那些其實是正確的「對象是」「自制力」）；`一周` 1 與 `呆在` 1 是刻意加的保護詞在起作用。

**驗證**：46 項回歸 ＋ 14 項新案例 ＋ 5 項守門負向測試全通過，另加 10 條反例測試（對象是／印象是／旋轉了一周／發呆在／這只是／強制要求／自制／形象／現象）全數維持原樣。

### [x] TODO-16：六位獨立稽核員的字典複審（2026-09-01）

前面幾輪的字典修改**都是同一個人改、同一個人驗**，違反「生成與審核要分離」。本輪改派 6 位獨立稽核員各帶一個視角全掃一次，結果由主線逐條覆核。

**素材**：轉換後全文 `converted.txt`（10.18 MB）、二字詞頻 39,390 種、三字詞頻 50,698 種、現有 2,873 條詞組與 2,706 條逐字規則。

**視角分派**（刻意不派 6 個相同的掃描，那只會得到 6 份相同的答案）：一簡多繁選字／兩岸用語／舊字形與異體字／**規則誤傷（方向相反，專找改壞的地方）**／人名與專有名詞／簡體殘留與標點量詞。

#### 採納並已修正（24 條規則）

| 發現 | 次數 | 處置 |
|---|---:|---|
| `這只貓` 應為 `這隻貓`（量詞義的「只」） | ~60 | 加 12 條副詞保護詞（`这只是`／`那只能`⋯）後開放 `这只→這隻`／`那只→那隻` |
| `卷入`／`卷进`／`被卷`／`席卷` 應為 `捲` | ~60 | 加 8 條動詞義規則。名詞義的問卷／試卷／膠卷不受影響，因為「卷」不在逐字表中 |
| `质量` 應為 `品質` | 18 | 語境全為品質義，非物理質量 |
| `公交` 應為 `公車` | 34 | `公交车→公車` 原已存在，但單獨的 `公交` 沒被涵蓋 |

**驗證**：19 條新案例（量詞轉對、副詞不受傷、名詞不受傷）全通過，另 46 項回歸 ＋ 14 項舊案例 ＋ 5 項守門負向測試全綠。

#### 🔴 誤報，不予採納（記在這裡，不要再重新提案）

| 稽核員的提案 | 為什麼不採納 |
|---|---|
| `姊妹 → 姐妹` | **照做反而會製造錯字**。「姊」是台灣標準字形，姊姊／姊妹都是正確寫法 |
| `蔵 → 藏`（冷蔵庫）、`裏 → 裡`、`啓 → 啟`（拝啓） | 覆核後確認**這三處全都在 CSV 的日文原文欄位裡**，不是畫面上的中文譯文 |
| 詞組規則 `樹里 → 樹里` 左側應改成簡體 | **那是刻意的**。說話者欄位是日文原文，本來就寫作漢字「樹里」，只擋簡體會漏（TODO-13 曾實測殘留一處錯字） |
| 「日文殘留超過千次，是結構性問題」 | **是萃取檔的假象**。實測：說話者欄位含假名的有 104,870 行，譯文欄位只有 507 行。稽核員掃到的是欄位，不是畫面 |

#### 🔴 本輪查證中發現的一件事實（會影響往後所有語料統計）

`corpus-extract.txt` **包含 CSV 的全部欄位，含日文原文那一欄**，不是只有中文譯文。

所以先前所有「語料出現 N 次」的數字都含日文欄位。對字典決策影響很小（詞組的 key 是簡體字串，日文欄位幾乎不會命中），但**往後要引用語料次數時必須知道這件事**。

#### ⬜ 待裁示（尚未處理）

| 提案 | 次數 | 我的看法 |
|---|---:|---|
| `占用 → 佔用` | 52 | **與既有裁示相關**：`占→佔` 是先前手動移除過的規則。若只加「占用」這個詞組不影響占卜（88 次）與占星，技術上安全，但要確認這不算推翻原裁示 |
| `便利店 → 便利商店` | 166 | 台灣正式說法是便利商店／超商。次數最多的一條 |
| `小姐姐 → 小姐／姐姐` | 16 | 大陸網路用語。改動會影響語氣，屬語感題 |
| 破折號 `——` | 33,218 | **建議不動**。那是譯者的行文風格，改它等於改翻譯 |
| `米`／`升` 單位 | 13 | **建議不動**。數字＋米只有 9 處，但「大米」5 處與「米飯」13 處是食物，泛用規則風險高於效益 |

### [x] TODO-09：實機驗證

**第一輪（引擎修復後）**：停用舊版腳本後單版本測試，Console 顯示字型載入與 Wi 攔截訊息各一次且行號對應本機版，遊戲運作正常、無腳本自身錯誤。

**第二輪（字典大改後）**：TODO-08 至 TODO-14 合計改動 8,500 處以上的可見文字（`里`／`裡` 7,147 處、`干`／`幹`／`乾` 1,474 處），實機確認**角色名「西城樹里」顯示正確、未變成「樹裡」**，其餘文字無異常。這是最該看的一項，因為它是泛用 `里` 規則唯一的破壞面。

**後續每次改字典仍需重驗**：`node --check` 只能證明語法正確，證明不了畫面正常。回歸測試也不行 —— 它只驗我想得到的案例。

---

## 本次一併處理的重大事故（已修復並上線）

`.user.js` 的區塊註解未正確閉合，導致第 25 行至第 129 行整段（含整個繁簡轉換引擎與字型注入）被吃進註解，腳本完全無法執行。損毀起點為 `b4d5be1`，與上述 `艺 → 豔` 錯位屬同一次編輯事故。已於 commit `5a75ae3` 單獨修復並推上 `main`，驗證方式為 `node --check` 前後對照（修復前 exit=1 `SyntaxError`，修復後 exit=0）。

---

## 裁示紀錄（2026-08-31）

以下三項已決定，**不要重新爭論**。之後若要推翻，請連同這裡的理由一起評估。

### 一、`干` 的預設值維持 `幹`

語料 1,474 處 `干` 的真實分布是 幹 1,077 ／ 乾 374 ／ 干 23，預設值改成 `幹` 後正確率由 37.4% 提升到 98.3%。

疑慮曾是「幹」在台灣語感偏粗俗。**取證後判定疑慮不成立**：把語料中每一處單獨出現的 `干` 撈出來看，全部是結巴表現（「幹、幹嘛」「幹、幹什麼啊」，日文口吃的譯法）、正面誇獎（「幹得漂亮」「幹·得·好」）或在講字形（「樹幹的『幹』字」）。**沒有一處是單獨當感嘆詞使用**，而那才是會被讀成髒話的位置。

保留這個判準：**看的是「單獨成句的 `幹`」，不是「`幹` 出現幾次」。** 之後若上游語料換版，重跑同一個檢查即可。

### 二、`干擾`／`皇后` 這組限制接受不修

`干` 與 `后` 本身是逐字表的 key，詞組保護不了它們（詞組輸出會被逐字階段再吃一次），所以「干擾」會顯示為「幹擾」、「皇后」會顯示為「皇後」。語料共 23 處、佔 1.6%。

修法是替 `s2t()` 加一個逐字轉換之後的詞組階段。**決定不做**：為 1.6% 動引擎架構不划算，且動了要重驗全部規則。留作已知限制。

另有一項純偏好、目前照原樣不動：`秘` 維持 `秘`（秘密／神秘／秘書），未改為教育部標準字 `祕`。OpenCC 的台灣變體表沒有改它，兩種在台灣都通行。

### 三、OpenCC 台灣用語詞庫的 27 條全部不套用

理由是這份詞庫本質為資訊工程術語表，而本遊戲的對話語境不會出現工程術語。逐條清單：

| 詞條 | 語料次數 | 不套用的理由 |
|---|---:|---|
| `支持 → 支援` | 430 | 語料全是「全力支持你」的應援義 |
| `消息 → 訊息` | 227 | 兼有「好消息」與「傳訊息」兩義，不能一律替換 |
| `类型 → 型別` | 109 | 語料是「那種類型的人」「服裝類型」 |
| `对象 → 物件` | 91 | 語料是「結婚對象」「練習對象」，全部指人 |
| `信息 → 資訊` | 91 | 對話中多為訊息義 |
| `文件 → 檔案` | 85 | 語料是「真相文件」，指紙本 |
| `项目 → 專案` | 78 | 語料是「比賽項目」「魔術項目」 |
| `菜单 → 選單` | 56 | 語料是「情人節菜單」，指餐廳菜單 |
| `保存 → 儲存` | 46 | 台灣同樣使用「保存」 |
| `高级 → 高階` | 41 | 語料是「高級和牛」「高級懷石」，指高檔 |
| `设备 → 裝置` | 39 | 台灣同樣使用「設備」 |
| `发送 → 傳送` | 35 | 台灣同樣使用「發送」 |
| `查看 → 檢視` | 32 | 🔴 語料有「查查看吧」，替換後成為「查檢視」，直接壞掉 |
| `数据 → 資料` | 25 | 台灣同樣使用「數據」 |
| `连接 → 連線` | 19 | 語料是「金屬連接起來」的一般連接義 |
| `循环 → 迴圈` | 13 | 語料是「循環利用」，迴圈是程式術語 |
| `扩展 → 擴充套件` | 11 | 對話語境不適用此技術譯法 |
| `运行 → 執行` | 9 | 對話語境不需替換 |
| `声明 → 宣告` | 8 | 語料是「退團聲明」「否認聲明」 |
| `添加 → 新增` | 8 | 對話中可能是「添加調味料」 |
| `用户 → 使用者` | 8 | 對話語境罕見，替換價值低 |
| `触摸 → 觸控` | 8 | 對話中多為觸碰他人，觸控是 UI 術語 |
| `脚本 → 指令碼` | 7 | 🔴 語料是「按照腳本」「分鏡腳本」「腳本家」，指劇本 |
| `指针 → 指標` | 7 | 對話中可能是鐘錶指針 |
| `窗口 → 視窗` | 6 | 對話中可能是窗戶或櫃檯窗口 |
| `程序 → 程式` | 5 | 語料是「下個程序」「走程序」，指流程 |
| `字体 → 字型` | 5 | 台灣兩者通用，替換價值低 |

**通則**：新增本土化規則前，先在語料中查它的實際語境。**詞庫說某詞在台灣叫什麼，跟這個遊戲裡它是什麼意思，是兩件事。**

---

## 研究紀錄：詞組輸出優先（評估後決定不做，2026-08-31）

留這一節是為了**不要再重新研究一次**。結論是不做，但過程中量到的數字有用。

### 限制的機制

`s2t()` 只有兩步：①詞組替換 ②逐字轉換。問題是**第②步不知道第①步做過什麼**，它會把詞組剛換上去的字再掃一遍。

| 步驟 | 字串 |
|---|---|
| 輸入 | `干扰` |
| ① 詞組（表中寫著 `干扰 → 干擾`） | `干擾` |
| ② 逐字（看到 `干`，查表得 `幹`） | **`幹擾`** |

**規則不是沒生效，是生效之後被踩掉。**

### 實測：有多少條詞組正在被覆寫

掃過全部詞組後，**有 75 條的輸出正在被第②步覆寫**：

| 被覆寫的字 | 影響條數 | 例 |
|---|---:|---|
| `斗 → 鬥` | 36 | 才高八**鬥** |
| `干 → 幹` | 17 | **幹**擾、大動**幹**戈 |
| `准 → 準` | 5 | 准不**準**許 |
| `后 → 後` | 3 | 大天**後**宮 |
| `台`／`托`／`仆`／`采`／`征`／`于`／`划`／`夸`／`叶`／`辟`／`腊` | 14 | 萬里長**徵**、前**僕**後繼 |

**但語料中真的會遇到的只有 1 條**（「沒精打采」1 次）。其餘全是成語與地名，遊戲對話用不到。加上 `干擾`／`干涉`／`干預` 那 23 處，**總影響約 24 處**。

🔴 **一個要記住的更正**：先前腳本註解寫的是「改架構這 75 條會壞掉」。**量完發現相反 —— 它們現在就已經壞了**，改架構是把它們修好。那是沒驗證就下的判斷。

### 三種修法與取捨

| 方案 | 做法 | 判定 |
|---|---|---|
| 甲：逐字之後再跑一次詞組 | 新增第三張表，key 為繁體 | ❌ 最差。兩張表 key 用不同文字系統，規則放錯表會靜默失效；字典格式、完整性檢查、負向測試全要長一套 |
| 乙：哨兵字元 | 詞組輸出先換佔位符，最後換回 | ❌ 失敗模式難看。任一環節漏還原，畫面會出現方塊亂碼 |
| **丙：詞組輸出說了算** | 改第②步的**作用範圍**：詞組換過的區段不准碰 | ✅ 最好。不新增表、不改字典格式、程式碼約 10 行 |

丙案的形狀：

```js
function s2t(text) {
  let out = '', last = 0;
  regexPhrases.lastIndex = 0;
  for (const m of text.matchAll(regexPhrases)) {
    out += charPass(text.slice(last, m.index));  // 詞組沒碰的部分才逐字轉
    out += phraseMap.get(m[0]);                  // 詞組的輸出原封不動
    last = m.index + m[0].length;
  }
  return out + charPass(text.slice(last));
}
```

丙案的附帶價值：目前每一支字典腳本裡都有「輸出含逐字表 key 就跳過」的防呆，那是在繞開這個限制。**丙案之後那段檢查整個不需要**，`干擾`／`干涉`／`干預`／`皇后`／`太后` 可以直接寫進詞組表。

### 為什麼決定不做

投報比不成立：只換得到約 24 處文字，卻要重跑一次完整實機驗證。**它的價值是未來的**（往後加詞組不必再想會不會被吃掉），不是現在的。

**若未來要做，做丙案，並在同一次施工裡完成：** ①改 `s2t()` ②寫掃描器複核那 75 條、抓出輸出仍含簡體字的（例如 `乾腊` 的 `腊`）③補一組負向測試證明覆寫不會再發生 ④重跑回歸與實機驗證。
