# 架構與備援策略

**這份文件回答三個問題**：這個腳本的哪些部分是自有資產、哪些是租來的？外部服務停掉時會怎樣？有什麼替代方案？

端點清單、存活實測與盤點方法在 [DEPENDENCIES.md](DEPENDENCIES.md)，本文不重複，只談架構與策略。

**成文日期**：2026-09-01（依據 `@version 1.13.8` 的程式碼與同期實測）

---

## 一、這是兩層，不是一層

```
遊戲 API（日文）
   ↓
① 漢化層 ── 日文 → 簡體中文        ← 資料抓自 www.shiny.fun（上游 biuuu 的服務）
   ↓
② 轉換層 ── 簡體 → 台灣正體 ＋ 字型   ← 完全內嵌於 .user.js，零連網
   ↓
畫面
```

**第①層的譯文不在檔案裡，是抓下來的。** 這是「腳本只是個呼叫器」這個印象的來源，對第①層而言正確。

**第②層不是。** `builder.js` 用 `JSON.stringify` 把整份字典寫死進 `.user.js`：

| 內容 | 位置 |
|---|---|
| 逐字表 2,714 字 | 內嵌 |
| 詞組表 2,814 條 | 內嵌 |
| 轉換引擎 `s2t()` | 內嵌 |
| 字型設定 | 內嵌（webfont 走 CDN，但有七層 fallback） |

→ **第②層斷網也能跑。這是本專案的自有資產。**

---

## 二、第①層內部其實有三段 fallback

一段劇情要顯示時，程式依序嘗試：

| 段 | 來源 | 形態 | 何時用 |
|---|---|---|---|
| ①-a | `www.shiny.fun/data/story/<id>.csv` | 人工翻譯，品質最好 | 一律優先 |
| ①-b | `ai.shiny.fun/story.json` → `/story/<t>.csv` | 預先生成的 AI 譯文靜態 CSV（實測索引 1,037 檔） | ①-a 沒有該檔，且「機翻」為 on |
| ①-c | `api.interpreter.caiyunai.com` | 即時翻譯，把當下日文台詞送出去（4KB 分塊） | ①-b 也沒有 |

### 🔴 三段全部吊在同一個地方

- ①-c 的**彩雲 token 來自 `manifest.json` 的 `cyweb_token`**（程式碼寫 `Gr.data.cyweb_token`）
- 供應商也由 manifest 的 `trans_api` 決定（實測值 `caiyun`，程式碼只有 `"caiyun" === e` 一個分支）

→ **manifest 拿不到 = 三段一起死**，不是只有 ①-a。

---

## 三、斷線時實際會發生什麼

| 情況 | 結果 |
|---|---|
| 有 `localStorage` 快取（常見） | 照常運作。快取的時效檢查因 `T.cacheTime` 未定義而永不觸發（見 DEPENDENCIES.md），舊譯文會一直用下去 |
| 無快取 ＋ 上游停站 | 畫面回到**日文**。第②層還在跑，但沒有簡體字可轉 |
| 上游停站 ＋ 有備份 | 切換來源即可照常運作 |

**精確的說法是：不是我們的程式碼壞掉，是它的原料斷了。**

### 降級狀態下的一個已知瑕疵（不修）

畫面留在日文時，第②層仍會對日文動手，因為**有幾個日文新字體與簡體字同形**：

| 日文原文 | 會變成 |
|---|---|
| 学園アイドル | **學**園アイドル |
| 団体で対応 | **團**體で対応 |

實測：`学`／`当`／`医` 在我們的逐字表中，`実`／`発`／`団`／`対`／`気` 不在。

**決定不修。** 這只發生在上游已斷線的情境，那時畫面本來就是日文；要修得在 `s2t()` 加語言判斷，成本遠高於效益。

---

## 四、切換備援來源的方法

### 方法一：選單（只能改 `origin`）

Tampermonkey 選單有「修改資料來源」，改的是 `T.origin`。

已驗證會生效：設定讀取（檔案位置 57512）發生在 `origin` 解構（74648）**之前**，自訂值不會被預設值蓋掉。

### 方法二：URL hash（`origin` 與 `ai_host` 都能改）

`F()` 解析 `location.hash`，以 `;` 分段、`=` 分鍵值，鍵名須在白名單內。白名單為 `["origin","ai_host","font1","font2","timeout","story","auto","bgm","dev","transCover"]`。

```
https://shinycolors.enza.fun/#ai_host=https://<自架位置>
```

→ **這是切換 `ai_host` 的唯一入口**，選單沒有提供。
※ 讀碼判定，未實機驗證。

---

## 五、替代方案評估

### ①-b（`ai.shiny.fun`）：最容易替代

它只是靜態檔。備份 `story.json` 與那 1,037 個 CSV，放上任何靜態主機，用 URL hash 指過去即可。**不必寫任何翻譯程式。**

### ①-c（彩雲即時翻譯）：可替代，但建議先不做

它是 fallback 的 fallback。沒有它，只是少數連 AI 譯文都沒有的劇情看不懂，而那些不是主要內容。

真要替代，方向如下。**可信度須註明**：除「瀏覽器內建」外，各家的免費額度與現況為 2026 年 5 月前的知識，**動工前一律重新實測**：

| 方向 | 特點 | 需金鑰 |
|---|---|---|
| 瀏覽器內建翻譯 API | 本機模型，零金鑰、零額度、可離線 | 否 |
| LibreTranslate（自架或公共實例） | 開源 | 自架不需 |
| DeepL API Free | 品質好，有月額度 | 是 |
| Gemini／OpenAI | 品質最好，可直接指定繁體 | 是 |
| 免費翻譯服務（MyMemory 等） | 匿名可用但額度小 | 多半不需 |

**若要做，建議先試瀏覽器內建**：不需金鑰、不需付費、不會撞額度，失敗成本最低。前提是先實測瀏覽器版本支不支援。

---

## 六、「直接日轉繁」的釐清

這個說法混了兩件不同的事：

### (a) 日文漢字 → 繁體字形轉換

可離線做，而且第②層現在就在做。但結果是「**學**園アイドル」這種半成品 —— 句子結構、假名、和語詞完全不通。

**這不是翻譯，是把字換掉。**

### (b) 日文 → 繁體中文翻譯

**需要翻譯引擎，機械對照表做不到。** 日文的語序、助詞、敬語層次沒有任何字典表處理得了。

→ 所以「直接日轉繁」等於「自己接一個翻譯 API」，回到第五節那張表。

### 若真的自接 API，仍建議輸出簡體再過我們的字典

理由是**品質歸屬**：「西城樹里不會變成樹裡」「幹勁不會變成乾勁」這些是我們自己保證的，字典是本專案唯一可控的品質。API 的輸出品質是租來的，不該讓它決定最終字形。

---

## 七、備援的優先序

| 順位 | 動作 | 理由 |
|---|---|---|
| **1** | 備份 `www.shiny.fun` 的 `manifest.json` 與 `/data/` | **一次解決三段**，因為彩雲 token 也在 manifest 裡 |
| 2 | 備份 `ai.shiny.fun` 的 `story.json` 與 1,037 個 CSV | 純檔案搬運，零程式 |
| 3 | 即時翻譯層的替代 | 最後手段的最後手段 |

**第 1 項完成後，本專案就沒有不可控的單點。** 備援的機制（切換來源）已經內建，缺的只有內容。

⬜ **尚未執行。** 開工前先做盤點與估算（依 manifest 的 `hashes` 欄位算出檔數與體積），報數字再決定是否下載。
